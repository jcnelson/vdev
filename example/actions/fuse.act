[vdev-action]
event=add
path=^fuse$
command=/bin/chown root:fuse "$VDEV_MOUNTPOINT/$VDEV_PATH" && /bin/chmod 0660 "$VDEV_MOUNTPOINT/$VDEV_PATH"; if [ -z "$(/bin/cat /proc/self/mounts | /bin/grep "fusectl")" ]; then /bin/mount -t fusectl fusectl /sys/fs/fuse/connections; fi
